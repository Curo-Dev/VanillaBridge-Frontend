import React, {
  FC,
  Fragment,
} from "react";
import {
  MainPage,
} from "src/components/pages";

export const App: FC = () => {
  return (
    <Fragment>
      <MainPage />
    </Fragment>
  );
}
