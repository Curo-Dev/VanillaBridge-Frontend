import React, {
  FC,
  Fragment,
} from "react";
import {
  MainTemplate,
} from "src/components/pages";

export const App: FC = () => {
  return (
    <Fragment>
      <MainTemplate />
    </Fragment>
  );
}
